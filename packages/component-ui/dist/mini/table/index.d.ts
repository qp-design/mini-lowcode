export declare const TableComponent: ({ dataPath, providerNum, consumeNum, expandable, height, type, ROWKEYY, columns, storeKey, rowSelectKey, ...restProps }: {
    storeKey: string;
    providerNum?: string;
    consumeNum?: string;
    expandable?: boolean;
    dataPath?: string;
    type?: "checkbox" | "radio" | "";
    rowSelectKey?: string;
    height?: number;
    columns: Array<any>;
    ROWKEYY: string;
}) => import("react/jsx-runtime").JSX.Element;
